server:
  port: 8761 #the default port used for eureka servers
  
logging:
  level:
    org:
      springframework:
        cloud: 
          netflix: trace
    com:
      rsystems: DEBUG
  file: C:/logs/eureka.log
  